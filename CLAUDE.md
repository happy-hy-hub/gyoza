# 03.gyoza

## 開発環境

| 項目 | 内容 |
| --- | --- |
| ターゲット | iPhone で動作するアプリケーション |
| 実働環境 | GitHub 上に上がったものを使用（ローカルビルド成果物は実働に使わない） |
| 開発マシン | Windows 11 (`C:\dev\03.gyoza`) |
| デバッグ | 実機 iPhone |
| 開発ツール | Claude Code（実装・差分管理） / Cowork（要件・調査・文書） / Chat（単発質問） |

### この構成から来る制約

- **Windows では iOS ネイティブビルドができない**（Xcode は macOS 専用）。
  実機に届ける経路は GitHub 経由になるため、以下のいずれかを技術調査フェーズで決定する:
  - PWA（Safari で開く / ホーム画面追加）— GitHub Pages 等でホスティング
  - React Native + Expo — EAS のクラウドビルド
  - Flutter + クラウド CI（Codemagic 等）
  - GitHub Actions の macOS ランナーでビルド → TestFlight 配信
- デバッグは実機のため、**Windows 側でのログ確認手段**も併せて決める必要がある。

### 未確定（要件定義フェーズで確定させる）

- 構成（ディレクトリ / モジュール分割）
- 命名規約
- 使用ツール・フレームワーク
- 判断基準（レビュー基準・完了の定義）

---

## 開発フロー管理

進め方は [doc/cowork-dev-workflow.md](doc/cowork-dev-workflow.md) に従う。
**このセクションは各フェーズの開始・完了時に必ず更新すること。**

| # | フェーズ | 担当 | 状態 |
| --- | --- | --- | --- |
| 0 | 準備（環境定義・ワークスペース設定） | 両方 | ✅ 完了 |
| 1 | 要件整理・仕様書 | **会議体（本リポジトリ）** | 🔵 進行中 |
| 2 | 技術調査・ライブラリ比較（配信経路の決定を含む） | Cowork | ⬜ 未着手 |
| 3 | アーキテクチャ決定（ADR）・構成／命名規約／判断基準の確定 | Cowork | ⬜ 未着手 |
| 4 | 実装・リファクタ・テスト | Claude Code | ⬜ 未着手 |
| 5 | テストデータ・検証ログ集計 | Cowork | ⬜ 未着手 |
| 6 | 単発のエラー質問・構文確認 | Chat | 随時 |
| 7 | README・手順書・図の更新 | Cowork | ⬜ 未着手 |

凡例: ⬜ 未着手 / 🔵 進行中 / ✅ 完了

### 現在地

- **フェーズ 1（要件整理）進行中** — 要件定義会議で論点を1つずつ処理中
- charter: **r3**（r2 の目的1書き換えは撤回済み。改訂履歴は charter 内に記録）
- 決定済み: ADR-001〜004（Q1 主対象ユーザー / Q2 中心機能 / Q5 配信経路 / Q3 再訪動機）
  - ADR-002 のタイマー要件は ADR-003 により改定済み
- 未決: **6件**（Q4 → Q10 → Q6 → Q7 → Q8 → Q9 の順）
- 終了条件: `open-questions.md` の未決が空 → `/spec` 実行

### 決まっていること（要約）

- 主対象は餃子を**作る人**。家庭での調理手順と記録。外部データ源はゼロ
- 中心機能は**調理中の手順ガイド**（タイマー内蔵）。焼き終わりに1タップ記録し、次回起動時に前回条件を初期値提示
- 記録の**一覧・振り返り（読み取り専用）**を足す。独立画面にはしない
- 配信は **PWA**（GitHub Pages・ホーム画面追加）。**追加料金ゼロが必須制約**
- タイマーは**前面表示＋Wake Lock で鳴る**。画面を離れると鳴らないのは仕様
- タイマー・通知・保存は**ポート抽象化**し、Web を一実装に留める

### フェーズ2（技術調査）への申し送り — 要実機検証

| # | 検証項目 | 不成立時 |
| --- | --- | --- |
| V1 | ホーム画面追加した PWA のストレージが、どの条件で OS に消去されるか | ADR-003 の再審議 |
| V2 | Screen Wake Lock API が実機で安定し、前面表示のままタイマーが鳴り切るか | ADR-002・003 の再審議 |

### 会議体の運用

| コマンド | 用途 |
| --- | --- |
| `/meeting <論点>` | 論点1つを5体に並列で諮り、争点のみ人間に提示 |
| `/decide <決定>` | 人間の裁定を ADR 化し open-questions を更新 |
| `/spec` | charter + 全 ADR から spec.md を生成 |

- 最終決定者は**人間のみ**。エージェントは助言のみで、多数決も採決もしない
- 拒否権による「反対」は 1 対 4 でも必ず争点として扱う
- 同一論点で 3 ラウンド収束しなければ「保留」で ADR に残し次へ進む

### 並列化のルール

- フェーズ 1（要件整理）と 2（技術調査）は同時に投げる
- フェーズ 4（実装）と 5（テストデータ生成）は同時に走らせる
- 逐次依存で投げると Cowork のサブエージェント並列性が死ぬ

---

## 運用メモ

- Cowork のモードは**手動**（調査・文書作成に自動は不要、使用量を余分に消費するため）
- ローカルファイル・ブラウザを使うタスク中は **Desktop アプリを開いたまま**にする
- 単発のエラー質問は Cowork ではなく **Chat** に逃がす
